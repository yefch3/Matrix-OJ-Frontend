/*
    This file is used to define the access control enum.
    The access control enum is used to define the access control for
 */
const ROLE_ENUM = {
  NOT_LOGIN: "NOT_LOGIN",
  USER: "USER",
  ADMIN: "ADMIN",
};

export default ROLE_ENUM;
